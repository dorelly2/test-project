<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<% @user.articles.each do |ff| %>
  <%= ff.title  %>
  <%= link_to "Edit", edit_article_path(ff) %>
<% end %>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
